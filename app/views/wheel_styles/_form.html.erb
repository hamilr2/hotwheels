<%= form_for(@wheel_style) do |f| %>
  <% if @wheel_style.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wheel_style.errors.count, "error") %> prohibited this wheel_style from being saved:</h2>

      <ul>
      <% @wheel_style.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :desc %><br />
    <%= f.text_field :desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
